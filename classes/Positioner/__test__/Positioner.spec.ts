describe('Test Positioner', () => {
  test('It should check if we can change vertical position of Figure', () => {})
  test('It should check if we can change horizontal position of Figure', () => {})
  test('It should check if we can shrink rows of pointstack', () => {})
  test('It should check if we have equalRows of pointsstack', () => {})
})
